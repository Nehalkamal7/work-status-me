{
  "buildCommand": "cd frontend 2>/dev/null || cd ../frontend 2>/dev/null || true; npm install && (chmod +x node_modules/.bin/* 2>/dev/null || true) && npx vite build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
